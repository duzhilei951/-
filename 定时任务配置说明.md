# AI模型每日监测 - 定时任务配置指南

## ✅ 配置步骤（只需执行一次）

### 第一步：以管理员身份打开命令提示符

1. 按 `Win + X` 键
2. 选择 **"终端(管理员)"** 或 **"Windows PowerShell (管理员)"**
3. 如果弹出UAC提示，点击"是"

### 第二步：复制并执行以下命令

```cmd
schtasks /create /tn "小远智-AI模型每日监测" /tr "PowerShell -ExecutionPolicy Bypass -File C:\Users\远智教育\.openclaw\workspace\小远智\scripts\daily_ai_report.ps1" /sc daily /st 09:00 /f
```

### 第三步：验证任务创建成功

```cmd
schtasks /query /tn "小远智-AI模型每日监测"
```

如果显示任务信息，说明创建成功！

---

## 📋 任务详情

| 项目 | 设置 |
|------|------|
| **任务名称** | 小远智-AI模型每日监测 |
| **执行时间** | 每天上午9:00 |
| **执行脚本** | `scripts/daily_ai_report.ps1` |
| **日志位置** | `logs/daily_report_YYYYMMDD.log` |
| **触发条件** | 仅在有新模型或重大升级时生成报告 |

---

## 🛠️ 常用管理命令

### 查看任务状态
```cmd
schtasks /query /tn "小远智-AI模型每日监测" /v
```

### 立即运行一次（测试）
```cmd
schtasks /run /tn "小远智-AI模型每日监测"
```

### 停止正在运行的任务
```cmd
schtasks /end /tn "小远智-AI模型每日监测"
```

### 禁用任务（暂停）
```cmd
schtasks /change /tn "小远智-AI模型每日监测" /disable
```

### 启用任务（恢复）
```cmd
schtasks /change /tn "小远智-AI模型每日监测" /enable
```

### 删除任务
```cmd
schtasks /delete /tn "小远智-AI模型每日监测" /f
```

---

## 🔍 查看执行结果

### 方法1：查看日志文件
日志保存在：`C:\Users\远智教育\.openclaw\workspace\小远智\logs\`

### 方法2：查看GitHub仓库
访问：https://github.com/duzhilei951/-

### 方法3：Windows事件查看器
1. 按 `Win + R`，输入 `eventvwr.msc`
2. 导航到：`应用程序和服务日志` → `Microsoft` → `Windows` → `TaskScheduler`
3. 查看操作日志

---

## ⚠️ 注意事项

1. **首次运行**：建议先手动运行一次测试
   ```cmd
   schtasks /run /tn "小远智-AI模型每日监测"
   ```

2. **网络依赖**：任务需要网络连接才能搜索新闻和推送GitHub

3. **API限制**：Tavily API有速率限制，日常使用不会触发

4. **修改时间**：如需调整执行时间，先删除再重新创建

---

## 📝 手动运行测试

如果想立即测试脚本是否正常工作：

```powershell
# 以普通用户身份打开PowerShell，执行：
cd C:\Users\远智教育\.openclaw\workspace\小远智\scripts
.\daily_ai_report.ps1
```

---

*配置完成后，系统会每天自动监测AI模型动态，有新模型或重大升级时自动生成报告并推送到GitHub。*
